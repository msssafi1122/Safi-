<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="10dp"
    app:cardBackgroundColor="#22FFFFFF"
    app:strokeColor="#10FFFFFF"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/tvNoteItemTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Note Title"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:layout_alignParentStart="true"
                android:layout_toStartOf="@+id/ivNoteItemPin"
                android:singleLine="true" />

            <ImageView
                android:id="@+id/ivNoteItemPin"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/star_on"
                android:tint="#FFB300"
                android:layout_alignParentEnd="true"
                android:visibility="gone"
                android:contentDescription="Pinned Item Indicator" />
        </RelativeLayout>

        <TextView
            android:id="@+id/tvNoteItemSnippet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Note details go here..."
            android:textColor="#B0FFFFFF"
            android:textSize="11sp"
            android:maxLines="2" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
